<h1 class="text-info">New Image</h1>

<% if params.has_key?(:comment_id)%>
  <%= render 'form', image: @image %>
<% else %>
  <%= render 'form_post', image: @image %>
<% end %>

<%= link_to 'Back', root_path %>
